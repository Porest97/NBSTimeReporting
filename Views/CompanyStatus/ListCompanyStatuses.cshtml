@model NBSTimeReporting.Models.ViewModels.SettingsViewModel
@{
    ViewData["Title"] = "ListCompanyStatuses";
}

<h1>ListCompanyStatuses</h1>

<a class="btn btn-sm btn-success" asp-controller="CompanyTypes" asp-action="Create">Create new status</a>
<a class="btn btn-sm btn-success" asp-controller="Home" asp-action="Companies">Back to Companies</a>

<table class="table table-responsive-sm table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompanyStatuses[0].CompanyStatusName)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.CompanyStatuses.OrderBy(p => p.CompanyStatusName))
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CompanyStatusName)
                </td>

                <td>
                    <a class="btn btn-sm btn-success" asp-controller="CompanyStatus" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-sm btn-success" asp-controller="CompanyStatus" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    <a class="btn btn-sm btn-danger" asp-controller="CompanyStatus" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
