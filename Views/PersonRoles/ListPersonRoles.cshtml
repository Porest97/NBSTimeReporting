@model NBSTimeReporting.Models.ViewModels.SettingsViewModel
@{
    ViewData["Title"] = "ListPersonRoles";
}

<h1>Person Roles</h1>

<a class="btn btn-sm btn-success" asp-controller="PersonRoles" asp-action="Create">Create new role</a>
<a class="btn btn-sm btn-success" asp-controller="Home" asp-action="People">Back to People</a>

<table class="table table-responsive-sm table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PersonRoles[0].PersonRoleName)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.PersonRoles.OrderBy(p => p.PersonRoleName))
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PersonRoleName)
                </td>

                <td>
                    <a class="btn btn-sm btn-success" asp-controller="PersonRoles" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-sm btn-success" asp-controller="PersonRoles" asp-action="Details" asp-route-id="@item.Id">Details</a>
                    <a class="btn btn-sm btn-danger" asp-controller="PersonRoles" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
